# C# Hello World

A simple "Hello, World!" console application in C# that waits for user input before closing.

## How to run

1. Install [.NET SDK](https://dotnet.microsoft.com/download)
2. Clone this repository
3. Run: `dotnet run Program.cs`
